# Reveal Line archive 16

Preserve immutable v0.57.2 from source `cc9f8ffcc3be246f53ca0df7f068476d8401d040`, tree `f6ff808b2d7cf60c4295f99e24e65fa9a26d9ed4`. `source-lock.json` binds its annotated tag, original metadata, distribution ZIP and exact source qualification. `input-authority.json` records the nine authenticated published release asset IDs/digests and distinguishes all pending archive authorities.

Historical v0.57.2 behavior remains intact, including file-input cancel, replay-export placement and Explorer history-return picture-writer limitations. The public review preserves these findings for a separately qualified corrective patch. This archive does not accept P01 or claim those defects are corrected. No future patch version/source is invented.

The original Archive15 preparation and verification tools are unchanged; committed-metadata checks now pin both exact v0.57.2 and v0.60.0 authorities. All synthetic corruption/refusal cases remain intact. Build only in a fresh hosted workspace with at least 3 GiB free: verify the exact clean pinned extractor, immutable release tag and all metadata; download both original ZIPs; validate every ZIP member, then reread every artifact body, including hidden files. Keep the 800,000,000-byte archive budget and all finite, no-clobber, link/path/corruption guards. Do not rebuild the game or alter any original asset.

Canonical play is available at `releases/v0.57.2/site/game/` and `releases/v0.60.0/site/game/`. Their original manifest rows and declared wrapper/metadata files form the pinned 1,347-file / 625,710,011-byte inventory. Manifest/checksum/build marker stay beneath `site/`; release and source qualification stay beside it. `/releases/` uses the unchanged accessible canonical-main Explorer bridge.

Main was bootstrapped without a workflow; these sixteen reviewed files follow a normal codex branch and PR/merge. Both workflow jobs are main-only. One main push triggers publication; serialized main-only workflow_dispatch remains available solely for a necessary retry after a recorded failure, never a duplicate dispatch. Configure Pages Actions and a main-only environment before merging the workflow.

Local checks use only tiny fixtures: `python3 -B -m unittest discover -s tools -p 'test_*.py' -v`, plus the exact pinned extractor corruption tests. Actual hosted receipts, all public HTTP/MIME/byte checks and a separately recorded native historical title→Deploy→capture→pause, Explorer/Back and portrait sample are required before an archive admission. No browser, offline, physical-device or public acceptance is implied by this candidate.

## Prepared v0.60.0 append

The added original v0.60.0 is frozen source `170508f11dd41204b7e24917a823f21701c15961`, tree `287ea0fddf97f594af0ce9eba452a24dc5f507c7`, annotated tag `03bf36ae9a36f5e776464bd5ef75043cfba727d7`. Its four small metadata/qualification bodies match the actual published asset digests. `input-authority-v0600.json` records these authorities separately from the unchanged earlier `input-authority.json`.

All 660 v0.57.2 canonical rows remain byte-identical. Only the archive's shared index changes among old deployed rows; the release-explorer bridge and production tools remain unchanged. The combined original artifact leaves 174,289,989 bytes below the unchanged 800,000,000-byte cap. Source/distribution payloads are not downloaded locally or rebuilt by this preparation.

This append is a prepared candidate, not a new archive admission. Root review, commit, the existing hosted main-push workflow, complete public-byte verification and scoped old/new edition browser observations remain required. The earlier v0.57.2 limitations remain historical facts; no P05, offline, physical-device or human-playtest completion is implied.
